name: Deploy to Production

on:
  push:
    branches: [ "main" ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: production
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3
      
    - name: Build Docker Image
      uses: docker/build-push-action@v7
      with:
        context: .
        push: false
        tags: erp-core:latest
        cache-from: type=gha
        cache-to: type=gha,mode=max
        
    # Example Step: Push to Docker Hub (Uncomment and configure secrets)
    # - name: Login to Docker Hub
    #   uses: docker/login-action@v3
    #   with:
    #     username: ${{ secrets.DOCKERHUB_USERNAME }}
    #     password: ${{ secrets.DOCKERHUB_TOKEN }}
    # - name: Push Docker Image
    #   uses: docker/build-push-action@v7
    #   with:
    #     context: .
    #     push: true
    #     tags: ${{ secrets.DOCKERHUB_USERNAME }}/erp-core:latest
    #     cache-from: type=gha
    #     cache-to: type=gha,mode=max
        
    # Example Step: Deploy via SSH (Uncomment and configure secrets)
    # - name: Deploy to Production Server
    #   uses: appleboy/ssh-action@master
    #   with:
    #     host: ${{ secrets.PROD_HOST }}
    #     username: ${{ secrets.PROD_USERNAME }}
    #     key: ${{ secrets.PROD_SSH_KEY }}
    #     script: |
    #       docker pull ${{ secrets.DOCKERHUB_USERNAME }}/erp-core:latest
    #       docker stop erp-core || true
    #       docker rm erp-core || true
    #       docker run -d -p 8080:8080 --name erp-core \
    #         -v /path/to/.env:/app/.env \
    #         -e SPRING_PROFILES_ACTIVE=prod \
    #         ${{ secrets.DOCKERHUB_USERNAME }}/erp-core:latest
