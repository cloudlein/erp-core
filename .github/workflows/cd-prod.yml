name: Deploy to Production

on:
  push:
    branches: [ "main" ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: production
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up JDK 21
      uses: actions/setup-java@v4
      with:
        java-version: '21'
        distribution: 'temurin'
        
    - name: Grant execute permission for gradlew
      run: chmod +x gradlew
      
    - name: Build JAR
      run: ./gradlew clean build -x test
      
    - name: Build Docker Image
      run: docker build -t erp-core:latest .
      
    # Example Step: Push to Docker Hub (Uncomment and configure secrets)
    # - name: Login to Docker Hub
    #   uses: docker/login-action@v3
    #   with:
    #     username: ${{ secrets.DOCKERHUB_USERNAME }}
    #     password: ${{ secrets.DOCKERHUB_TOKEN }}
    # - name: Push Docker Image
    #   run: |
    #     docker tag erp-core:latest ${{ secrets.DOCKERHUB_USERNAME }}/erp-core:latest
    #     docker push ${{ secrets.DOCKERHUB_USERNAME }}/erp-core:latest
        
    # Example Step: Deploy via SSH (Uncomment and configure secrets)
    # - name: Deploy to Production Server
    #   uses: appleboy/ssh-action@master
    #   with:
    #     host: ${{ secrets.PROD_HOST }}
    #     username: ${{ secrets.PROD_USERNAME }}
    #     key: ${{ secrets.PROD_SSH_KEY }}
    #     script: |
    #       docker pull ${{ secrets.DOCKERHUB_USERNAME }}/erp-core:latest
    #       docker stop erp-core || true
    #       docker rm erp-core || true
    #       docker run -d -p 8080:8080 --name erp-core \
    #         -e SPRING_PROFILES_ACTIVE=prod \
    #         -e DB_HOST=${{ secrets.PROD_DB_HOST }} \
    #         -e DB_PASSWORD=${{ secrets.PROD_DB_PASSWORD }} \
    #         ${{ secrets.DOCKERHUB_USERNAME }}/erp-core:latest
