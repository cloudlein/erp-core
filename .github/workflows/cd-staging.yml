name: Deploy to Staging

on:
  push:
    branches: [ "staging" ]

jobs:
  deploy:
    runs-on: ubuntu-latest
    environment: staging
    steps:
    - uses: actions/checkout@v4
    
    - name: Set up Docker Buildx
      uses: docker/setup-buildx-action@v3
      
    - name: Build Docker Image
      uses: docker/build-push-action@v5
      with:
        context: .
        push: false
        tags: erp-core:staging
        cache-from: type=gha
        cache-to: type=gha,mode=max
        
    # Example Step: Push to Docker Hub (Uncomment and configure secrets)
    # - name: Login to Docker Hub
    #   uses: docker/login-action@v3
    #   with:
    #     username: ${{ secrets.DOCKERHUB_USERNAME }}
    #     password: ${{ secrets.DOCKERHUB_TOKEN }}
    # - name: Push Docker Image
    #   uses: docker/build-push-action@v5
    #   with:
    #     context: .
    #     push: true
    #     tags: ${{ secrets.DOCKERHUB_USERNAME }}/erp-core:staging
    #     cache-from: type=gha
    #     cache-to: type=gha,mode=max
        
    # Example Step: Deploy via SSH (Uncomment and configure secrets)
    # - name: Deploy to Staging Server
    #   uses: appleboy/ssh-action@master
    #   with:
    #     host: ${{ secrets.STAGING_HOST }}
    #     username: ${{ secrets.STAGING_USERNAME }}
    #     key: ${{ secrets.STAGING_SSH_KEY }}
    #     script: |
    #       docker pull ${{ secrets.DOCKERHUB_USERNAME }}/erp-core:staging
    #       docker stop erp-core-staging || true
    #       docker rm erp-core-staging || true
    #       docker run -d -p 8081:8080 --name erp-core-staging \
    #         -v /path/to/.env:/app/.env \
    #         -e SPRING_PROFILES_ACTIVE=staging \
    #         ${{ secrets.DOCKERHUB_USERNAME }}/erp-core:staging
